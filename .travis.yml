go_import_path: goji.io
language: go
sudo: false

matrix:
  include:
    - go: 1.5
    - go: tip

script:
  - go get -t -v ./...
  - diff -u <(echo -n) <(gofmt -d .)
  - go tool vet .
  - go test -v -cover -race ./...
